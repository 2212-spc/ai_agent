# Vue前端开发环境配置指南 🚀

## 📋 第一步：安装必需软件

### 1. 安装 Node.js（必需）

**推荐版本：Node.js 18 或更高**

#### Windows 安装步骤：

1. **下载**：访问 https://nodejs.org/
2. **选择版本**：
   - 推荐下载 **LTS版本**（长期支持版，更稳定）
   - 例如：Node.js 20.x LTS
3. **安装**：
   - 双击下载的 `.msi` 文件
   - 一路 "Next"，保持默认设置
   - **勾选** "Automatically install the necessary tools"
4. **验证安装**：
   - 打开 **PowerShell** 或 **命令提示符**
   - 输入以下命令：
   ```bash
   node -v
   # 应该显示：v20.x.x（或类似版本号）
   
   npm -v
   # 应该显示：10.x.x（或类似版本号）
   ```

如果显示版本号，说明安装成功！✅

---

## 📦 第二步：安装项目依赖

### 1. 打开项目目录

在 **PowerShell** 或 **命令提示符** 中：

```bash
cd "d:\PycCharm Code\AI-Agent\ai_agent\frontend-vue"
```

### 2. 安装依赖包

运行以下命令（首次运行需要几分钟）：

```bash
npm install
```

或者使用更快的 **pnpm**（可选）：

```bash
# 如果没有pnpm，先安装它
npm install -g pnpm

# 然后用pnpm安装依赖
pnpm install
```

**等待安装完成**，你会看到：
```
added 312 packages in 2m
```

---

## 🚀 第三步：启动开发服务器

### 1. 运行开发服务器

```bash
npm run dev
```

### 2. 查看运行结果

启动成功后，你会看到：

```
  VITE v7.2.4  ready in 500 ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: http://192.168.x.x:5173/
  ➜  press h + enter to show help
```

### 3. 打开浏览器

- **方法1**：按住 `Ctrl` 并点击 `http://localhost:5173/`
- **方法2**：直接在浏览器中输入 `http://localhost:5173/`

🎉 **Vue前端已启动！**

---

## 🛠️ 第四步：推荐的开发工具

### 1. VS Code 插件（强烈推荐）

安装以下插件让Vue开发更轻松：

1. **Vue - Official**（必装！）
   - 官方Vue插件，提供语法高亮、代码补全
   - 扩展ID：`Vue.volar`

2. **Vue VSCode Snippets**
   - 代码片段，快速生成模板
   - 扩展ID：`sdras.vue-vscode-snippets`

3. **ESLint**（代码规范）
   - 扩展ID：`dbaeumer.vscode-eslint`

4. **Prettier - Code formatter**（代码格式化）
   - 扩展ID：`esbenp.prettier-vscode`

5. **Auto Rename Tag**（标签自动重命名）
   - 扩展ID：`formulahendry.auto-rename-tag`

### 2. 浏览器插件

安装 **Vue DevTools**（调试神器）：

- **Chrome**：https://chrome.google.com/webstore → 搜索 "Vue DevTools"
- **Edge**：https://microsoftedge.microsoft.com/addons → 搜索 "Vue DevTools"
- **Firefox**：https://addons.mozilla.org → 搜索 "Vue DevTools"

---

## 📂 项目结构说明

```
frontend-vue/
├── src/
│   ├── views/           # 页面组件
│   │   ├── AgentChat.vue       # 💬 聊天页面（主要页面）
│   │   ├── Login.vue           # 🔐 登录页面
│   │   └── Register.vue        # 📝 注册页面
│   ├── components/      # 可复用组件
│   │   ├── Chat/
│   │   │   ├── ChatPanel.vue      # 聊天面板
│   │   │   └── TimelinePanel.vue  # 时间线面板
│   │   └── NotificationContainer.vue
│   ├── stores/          # Pinia状态管理（重要！）
│   │   ├── chat.js            # 💾 聊天状态和逻辑
│   │   └── canvas.js
│   ├── router/          # 路由配置
│   │   └── index.js
│   ├── assets/          # 静态资源（CSS、图片）
│   └── main.js          # 入口文件
├── package.json         # 依赖配置
└── vite.config.js       # Vite配置
```

---

## 🐛 开始修改Bug

### 修改流程：

1. **找到对应文件**：
   - 聊天相关bug → `src/stores/chat.js` 或 `src/views/AgentChat.vue`
   - 记忆问题 → `src/stores/chat.js` 的 `sendMessage` 函数

2. **修改代码**：
   - Vite 支持**热更新**（HMR）
   - 保存文件后，浏览器会**自动刷新**
   - 无需手动重启！

3. **查看效果**：
   - 保存文件
   - 浏览器自动更新
   - 查看控制台日志

4. **调试技巧**：
   - 打开浏览器 F12 → Console 查看日志
   - Vue DevTools → 查看组件状态
   - `console.log()` 调试

---

## 🔧 常用命令

```bash
# 启动开发服务器（热更新）
npm run dev

# 构建生产版本
npm run build

# 预览构建结果
npm run preview

# 清理缓存（遇到奇怪问题时）
rm -rf node_modules package-lock.json
npm install
```

---

## ⚠️ 常见问题解决

### 问题1：端口被占用

```
Error: Port 5173 is already in use
```

**解决**：
- 关闭其他占用5173端口的程序
- 或修改 `vite.config.js`：
  ```js
  export default {
    server: {
      port: 5174  // 改成其他端口
    }
  }
  ```

### 问题2：模块未找到

```
Error: Cannot find module 'xxx'
```

**解决**：
```bash
npm install
```

### 问题3：npm安装很慢

**解决**：切换国内镜像
```bash
npm config set registry https://registry.npmmirror.com
npm install
```

### 问题4：保存后没有自动刷新

**解决**：
- 检查是否有编译错误（查看终端）
- 重启开发服务器：`Ctrl+C` 然后 `npm run dev`

---

## 🎯 下一步：对比HTML版本修bug

### Vue版本的bug修复要点：

1. **用户ID传递**（已经在HTML版本修复）
   - 文件：`src/stores/chat.js`
   - 位置：`sendMessage` 函数
   - 需要：添加 `user_id: localStorage.getItem('user_id')`

2. **记忆模式控制**
   - 文件：`src/stores/chat.js`
   - 添加：`share_memory` 参数

3. **深度思考事件监听**
   - 文件：`src/stores/chat.js`
   - 需要：监听 `agent_thought`、`agent_observation` 事件

4. **思考步骤持久化**
   - 需要：类似HTML版本的localStorage保存逻辑

---

## 📚 学习资源

- **Vue 3 官方文档**：https://cn.vuejs.org/
- **Vite 官方文档**：https://cn.vitejs.dev/
- **Pinia 状态管理**：https://pinia.vuejs.org/zh/

---

## ✅ 验证清单

在开始修改前，确保：

- [ ] Node.js 已安装（`node -v` 有输出）
- [ ] 依赖已安装（`node_modules` 文件夹存在）
- [ ] 开发服务器已启动（浏览器能打开 http://localhost:5173/）
- [ ] Vue DevTools 已安装（浏览器插件）
- [ ] VS Code 已安装 Vue - Official 插件

**全部完成？开始修bug吧！** 🚀

---

## 💡 提示

- **保存即生效**：Vue + Vite 的热更新非常快
- **多看控制台**：F12 → Console，所有错误都在这里
- **参考HTML版本**：两个版本的逻辑是一样的，只是写法不同
- **不懂就问**：遇到Vue语法不理解的地方，随时问我！

**祝开发顺利！** 🎉

